# 生活小秘 · 云端同步接入说明

> 范围：P0「真后端 AI + 云端可靠提醒」落地交付。
> 目标：把原本的内存伪后端升级为持久化、带账号体系、跨端可靠的真实后端；登录用户的数据换机/重装不丢，安卓端关屏杀进程仍能按时响铃。

---

## 一、总体架构

```
┌─────────────┐      HTTPS / JSON      ┌──────────────────────────┐
│  Android App │ ──── NativeBridge ───▶ │                          │
│ (WebView+H5) │ ◀── setCloudConfig ──  │      Node 后端 server/    │
└─────────────┘                        │  · store.js  JSON 持久化  │
        │                               │  · auth.js  Bearer Token │
        │ 加载本地 H5                    │  · 按 userId 数据隔离     │
        ▼                               └──────────────────────────┘
┌─────────────┐                                ▲
│  H5 前端 app/ │ ──────── API (fetch) ────────┘
│  · api.js     │   注册/登录/云端提醒同步/AI
└─────────────┘
```

- **后端**：零依赖 Node（`server/`），JSON 文件持久化，账号鉴权，提醒/行程按用户隔离。
- **H5**：Web 前端（`app/`），登录后「云端为准」，本地变更回写云端（全量替换，幂等）。
- **Android**：`MainActivity` 内嵌 WebView 加载本地 H5；`CloudSync` 在后台拉取云端提醒并交给 `ReminderScheduler` 调度系统闹钟。

---

## 二、后端（`server/`）

### 2.1 持久化（`store.js`）
- 文件：`server/data/db.json`（首次启动自动创建）。
- 结构：`{ users, reminders, trips }`，全部按 `userId` 隔离。
- 密码：`pbkdf2(100000, sha256)` + 随机盐；比对用 `crypto.timingSafeEqual` 防时序攻击。

### 2.2 账号与鉴权（`auth.js`）
- Token：`HMAC-SHA256` 签名，`base64url` 编码，有效期 **30 天**（`TOKEN_TTL_MS`）。
- 解析：`Authorization: Bearer <token>` → `getUid(req)`。
- **生产务必设置环境变量 `JWT_SECRET`**，否则使用内置默认值（不安全）。

### 2.3 接口清单

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET  | `/api/health` | 健康检查（含 `persistent:true` 与端点列表） | 否 |
| POST | `/api/auth/register` | 注册 `{identifier,password,name?}` → `{token,user}` | 否 |
| POST | `/api/auth/login` | 登录 `{identifier,password}` → `{token,user}` | 否 |
| GET  | `/api/me` | 当前用户 | 是 |
| GET  | `/api/reminders` | 拉取该用户全部提醒 `{reminders:[...]}` | 是 |
| POST | `/api/reminders` | `{reminders:[...]}` 全量替换同步（幂等）或单条创建 | 是 |
| PUT/DELETE | `/api/reminders/:id` | 更新/删除单条 | 是 |
| GET/POST/DELETE | `/api/trips` | 云端行程基础 CRUD | 是 |
| POST | `/api/ai/chat` | AI 对话，登录用户创建的提醒/行程落其云端空间 | 否（可带 token） |

- CORS：`Access-Control-Allow-Origin: *`，支持跨域前后端分离部署。

### 2.4 数据隔离与 AI 落云
- 提醒/行程全部带 `userId`；`replaceReminders(userId, list)` 用完整列表**幂等替换**，避免逐条并发冲突。
- `ai-engine.chat(message, sessionId, userId)`：当 `userId` 存在时，AI 自动创建的提醒/行程写入云端 `store`；未登录则回退内存演示库。

---

## 三、H5 前端（`app/`）

### 3.1 API 客户端（`app/js/api.js`）
- 方法：`init / setServer / setToken / register / login / logout / getMe / cloudPullReminders / cloudSyncReminders`。
- 登录态持久化：`localStorage` 的 `life_token` 与 `life_server`；刷新页面自动恢复登录态。

### 3.2 本地 ↔ 云端 混合策略
- **未登录**：数据存本地 `localStorage`（演示态）。
- **已登录**：以云端为准；本地增删改后调用 `cloudSyncReminders(REMINDERS)` 全量回写云端。
- **Android 内**：登录成功后额外调用 `window.AndroidBridge.setCloudConfig(server, token)`，把后端地址与 token 交给原生，触发原生拉取并调度系统闹钟（关屏/杀进程仍响）；退出登录调用 `clearCloudConfig()`。

---

## 四、Android 原生（`android-app/`）

### 4.1 `CloudSync.kt`
- 后台线程 `GET /api/reminders`（`Authorization: Bearer <token>`）→ 映射 → `ReminderScheduler.syncAll(ctx, json)`。
- **关键：云端提醒 `id` 是字符串**（如 `cloud_1`），而原生 `ReminderScheduler` 用 `optLong("id")` 派生 `AlarmManager` 的 `requestCode`，字符串会全部变成 0 导致多条提醒互相覆盖。因此 `CloudSync` 把字符串 id 派生为**稳定的 31 位正整数**（`String.hashCode() & 0x7FFFFFFF`），保证 AlarmManager code 唯一且跨同步稳定。
- 触发时机：`MainActivity.onCreate` / `onResume`、`BootReceiver`（设备重启/应用更新）、H5 登录后 `setCloudConfig` 主动调用。
- token 与 server 存 `SharedPreferences`（`life_cloud`）；生产建议改用 `EncryptedSharedPreferences`。

### 4.2 `NativeBridge` 对接（`MainActivity.kt`）
- `setCloudConfig(server, token)`：保存配置并立即 `CloudSync.sync`。
- `clearCloudConfig()`：退出登录时清空，原生回到纯本地调度。

### 4.3 权限与配置（`AndroidManifest.xml`）
- 已声明：`INTERNET`、`ACCESS_NETWORK_STATE`、`RECEIVE_BOOT_COMPLETED`、`SCHEDULE_EXACT_ALARM`、`POST_NOTIFICATIONS`、`SEND_SMS`。
- `android:usesCleartextTraffic="true"`：允许 http（内网/自建后端调试用，生产建议 HTTPS）。
- 注意：**当前环境无 Android SDK，无法本地编译 APK**，需在装有 Android Studio / SDK 的机器上 `./gradlew assembleDebug` 重新构建。

---

## 五、部署

### 5.1 本地一键运行
```bash
cd server
node server.js            # 默认 3000，可用 PORT 环境变量覆盖
```
浏览器访问 `http://localhost:3000`（后端同时托管 `../app` 静态前端）。

### 5.2 公网部署
- **同源部署（推荐）**：后端单进程同时托管 `app/` 与 API（已在 `server.js` 内置）。在云主机 `node server.js` 后，`http://服务器IP:3000` 即前端、`/api/*` 即接口，**免 CORS、H5 无需填服务器地址**。详细见《后端公有云部署指南》。
- **前后端分离部署**：前端走 CloudStudio 静态站，后端在云主机运行 `node server.js`，设置 `JWT_SECRET`（必改）与 `PORT`，建议 Nginx 反代 + HTTPS；H5 内「云端同步」填后端公网地址，前端经 CORS（`*`）直连，Android 在 H5 登录时自动把该地址（同源时取页面 `origin`）传给原生。

### 5.3 客户端使用
1. 打开 H5（CloudStudio 链接或本地 `localhost:3000`）。
2. 设置 → 云端同步 → 填写服务器地址 → 注册/登录。
3. 登录后：AI 创建的提醒/行程自动落云端；安卓端同时获得系统级可靠提醒。

---

## 六、安全注意事项
- **`JWT_SECRET` 必须在生产环境通过环境变量覆盖**，否则 token 可被伪造。
- Token 目前明文存储于 `localStorage` / `SharedPreferences`；正式发布应使用加密存储。
- 建议全链路 HTTPS（避免 token 在传输中被嗅探）。

---

## 七、测试覆盖
- 后端接口：**19/19** 通过（health 持久化标记、注册/重复注册 409/错误密码 401/登录、`/api/me` 鉴权、提醒空列表、全量同步、带 userId 读取、磁盘持久化、单条创建/更新/删除、用户隔离、AI 落云）。
- H5 API 客户端：**6/6** 通过（setServer 归一化、注册登录 `isCloud`、全量同步、拉取一致、重载恢复登录态、重载后仍能拉取）。
- 接口冒烟（本次）：`register` → `login` 取 token → `GET /api/reminders`（空）✓ → `POST` 全量同步（字符串 id）✓ → `GET` 返回同一字符串 id ✓。

---

## 八、已知限制 / 后续
- Android 需本地 rebuild APK（本环境无 SDK）。
- 短信/微信提醒为能力占位，原生端目前统一走系统闹钟兜底（`method !== alarm` 时退化为精确闹钟）。
- 后续可加：token 刷新、加密存储、WebSocket 实时同步、服务端推送。
